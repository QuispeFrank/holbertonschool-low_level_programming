#include "main.h"
#include <stdio.h>

/**
 * mul - Determine if a character is a number
 * @a: value to test
 * @b: value to test
 * Return: ...
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
